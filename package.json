{
  "name": "bitburner-scripts-cicd",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "scripts": {
    "server": "node server/index.js",
    "build": "tsc && npm run pack",
    "pack": "node server/build.js",
    "start": "npm run pack && npm run server",
    "start-ts": "npm run build && npm run server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Derek Spiner",
  "license": "ISC",
  "dependencies": {
    "@types/express": "^4.17.13",
    "cors": "^2.8.5",
    "express": "^4.17.2",
    "uuid": "^8.3.2"
  },
  "devDependencies": {
    "@types/node": "^17.0.4"
  }
}
